import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
{% if values.includePWA %}import { VitePWA } from 'vite-plugin-pwa'
{% endif %}

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [
    react(){% if values.includePWA %},
    VitePWA({
      registerType: 'autoUpdate',
      workbox: {
        globPatterns: ['**/*.{js,css,html,ico,png,svg}']
      },
      includeAssets: ['favicon.ico', 'apple-touch-icon.png', 'masked-icon.svg'],
      manifest: {
        name: '${{ values.name }}',
        short_name: '${{ values.name }}',
        description: '${{ values.description }}',
        theme_color: '#ffffff',
        icons: [
          {
            src: 'pwa-192x192.png',
            sizes: '192x192',
            type: 'image/png'
          },
          {
            src: 'pwa-512x512.png',
            sizes: '512x512',
            type: 'image/png'
          }
        ]
      }
    }){% endif %}
  ],
  server: {
    port: 5173,
    open: true
  },
  build: {
    outDir: 'dist',
    sourcemap: true
  }
})